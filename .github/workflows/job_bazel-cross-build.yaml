name: Bazel Cross-Compilation Build
run-name: ${{ github.actor }} is cross-compiling with bazel
on:
  workflow_call:
    inputs:
      runner:
        description: 'Which runner to use'
        required: false
        default: 'ubuntu-24.04'
        type: string
      platform:
        description: 'Target platform (e.g. //third-party/bazel/toolchains:aarch64-linux-gnu)'
        required: true
        type: string
      platform_name:
        description: 'Human-readable platform name for display'
        required: true
        type: string
jobs:
  bazel-cross-build:
    name: Cross-compile (${{ inputs.platform_name }})
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: Mount bazel cache
        uses: actions/cache@v3
        with:
          path: "~/.cache/bazel"
          key: ${{ runner.os }}-bazel-cross-${{ inputs.platform_name }}-${{ hashFiles('.bazelversion', '.bazelrc', 'MODULE.bazel', 'third-party/bazel/*') }}
          restore-keys: |
            ${{ runner.os }}-bazel-cross-${{ inputs.platform_name }}-
            ${{ runner.os }}-bazel-
      - name: Build all
        run: >
          bazelisk build //...
          --platforms=${{ inputs.platform }}
          --@bazel_tools//tools/test:incompatible_use_default_test_toolchain=false
