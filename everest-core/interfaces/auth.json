{
    "description": "Interface of authentication framework",
    "cmds": {
        "authorize": {
            "description": "This returns a validated auth token or null if none could be validated",
            "result": {
                "description": "The auth token that can be used for signing etc. This will return null if no token could be validated.",
                "type": ["null", "string"],
                "minLength": 1,
                "maxLength":  20
            }
        }
    },
    "vars": {
        "authorized": {
            "description": "New validated auth token provided",
            "type": "string",
            "minLength": 1,
            "maxLength":  20
        }
    }
}
