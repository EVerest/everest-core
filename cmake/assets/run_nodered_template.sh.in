cd @EVEREST_DOCKER_DIR@
docker build -t everest-nodered .
docker run \
    --rm \
    --network host \
    --name everest_nodered \
    -e FLOWS=flows/@FLOW_FILE_NAME@ \
    --mount type=bind,source=@FLOW_FILE_DIRECTORY@,target=/data/flows \
    everest-nodered
