description: API for isolation monitors
config:
  cfg_communication_check_to_s:
    description: "Moximum time between to two communication check events and to reply to any request. Values <= 0 disables communication checks"
    type: integer
    default: 5
  cfg_heartbeat_interval_ms:
    description: "Interval between two heartbeat messages send by the API. Values <= 0 disable heartbeat"
    type: integer
    default: 1000

provides:
  if_isolation_monitor:
    interface: isolation_monitor
    description: "API for isolation monitors"
enable_external_mqtt: true
metadata:
  license: https://opensource.org/licenses/Apache-2.0
  authors:
    - Cornelius Claussen
    - Jan Christoph Habig
    - Florin Mihut
    - Miriam Thome
