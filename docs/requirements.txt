# Main Sphinx library
sphinx

# The Furo theme
furo

# Provides modern design elements like grids, cards, and tabs
sphinx-design

# Adds a "copy" button to code blocks for a better user experience
sphinx-copybutton

# Optional but highly recommended for local development.
# It automatically rebuilds the site when you save a file.
sphinx-autobuild